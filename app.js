const container=document.querySelector(".container")
const signupButton=document.querySelector(".fondo button")

signupButton.addEventListener("click",()=>{
    container.classList.toggle("change")
})